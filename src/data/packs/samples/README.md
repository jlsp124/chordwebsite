# Sample Pack Fixtures

This folder contains small hand-authored JSON fixtures used by `scripts/validate-packs.mjs`.

These files are:
- validator fixtures only
- Roman-numeral/function-first examples
- not shipped runtime assets
- not a raw corpus

The canonical runtime output path remains `public/packs/`.
The canonical authoring source path remains `data-src/`.
